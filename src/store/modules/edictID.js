
import * as types  from '../mutation-types';
const state = {
       
}

const mutations= {
    save_userInformation(state,s){
        state.prefecture=s
    }
}
export default {
    state,
    mutations
}

